

export const ClientProfileService = {
  // Generic function to get data from a collection
  // getData: async (collectionName) => {
  //   const snapshot = await firestore.collection(collectionName).get();
  //   return snapshot.docs.map((doc) => ({ id: doc.id, ...doc.data() }));
  // },

  // // Generic function to add data to a collection
  // addData: async (collectionName, data) => {
  //   await firestore.collection(collectionName).add(data);
  // },

  // // Generic function to update data in a document
  // updateData: async (collectionName, docId, newData) => {
  //   await firestore.collection(collectionName).doc(docId).update(newData);
  // },

  // // Generic function to delete a document from a collection
  // deleteData: async (collectionName, docId) => {
  //   await firestore.collection(collectionName).doc(docId).delete();
  // },
};

